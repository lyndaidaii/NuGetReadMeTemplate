on:
  push:
    branches:
      - main

jobs:
  your_job_name:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Create Github Issue
        uses: dacbd/create-issue-action@v1.2.1
        with:
          token: ${{ github.token }}
          title: Simple test issue
          body: my new issue
          owner: lyndaidaii
          repo: NuGetReadMeTemplate
